spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/fpl_assistant
    username: fpl
    password: fpl_secret
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
  ai:
    anthropic:
      api-key: ${ANTHROPIC_API_KEY}
      chat:
        options:
          model: claude-haiku-4-5-20251001

fpl:
  base-url: https://fantasy.premierleague.com/api
  team-id: 3257331