services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: fpl_assistant
      POSTGRES_USER: fpl
      POSTGRES_PASSWORD: fpl_secret
    ports:
      - "5432:5432"
    volumes:
      - fpl_data:/var/lib/postgresql/data

volumes:
  fpl_data:
